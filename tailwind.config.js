/** @type {import('tailwindcss').Config} */
module.exports = {
  //let tailwind know the files to it should look at to apply it's classes
  content: [
    "./app/**/*.{js,ts,jsx,tsx}"
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
